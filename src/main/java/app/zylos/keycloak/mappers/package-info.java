/**
 * Zylos Keycloak protocol mappers.
 *
 * <p>{@link app.zylos.keycloak.mappers.ActClaimMapper} populates the RFC
 * 8693 {@code act} claim on tokens issued via Standard Token Exchange V2.
 * Single-hop only; see ADR 0001 for the full design.
 */
package app.zylos.keycloak.mappers;
